spring.application.name=mallna
spring.datasource.url=jdbc:postgresql://192.168.1.19:5432/db_amatun
spring.datasource.username=postgres
spring.datasource.password=rami

# Pilote JDBC
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA (Hibernate) configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Client URLs
# Local dev URL (commented out)
# app.client.url=http://192.168.1.19:3000

# Production URLs
app.client.url=https://amatun.com
spring.mvc.cors.allowed-origins=https://amatun.com

# Upload directory
app.upload.dir=/app/uploads

# CORS settings
spring.mvc.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.mvc.cors.allowed-headers=*
spring.mvc.cors.allow-credentials=true

# Logging level
logging.level.org.springframework=DEBUG

# Server port fallback
server.port=${PORT:8081}

# Datasource and servlet multipart config
spring.datasource.hikari.auto-commit=false
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB

# SMTP Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=feresbrahmi@gmail.com
spring.mail.password=cbdt lgwn krps zgyc
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
